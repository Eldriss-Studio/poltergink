/**
 * `poltergink` — an LLM-native TypeScript wrapper around inkjs.
 *
 * @remarks
 * This entry point is intentionally empty at v0.0.0. The public API surface
 * (`Story`, `Player`, `Persona`, `Session`, `Transcript`) lands in subsequent
 * tasks. See {@link https://github.com/Eldriss-Studio/poltergink | the repo}
 * and the ADRs under `docs/decisions/` for the design contract.
 *
 * @packageDocumentation
 */

export const VERSION = "0.0.0";
